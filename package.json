{
  "name": "liga-ping-pong",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Grand Slam Ping Pong Series — app local para llevar la liga",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "deploy": "vite build && firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "firebase-tools": "^15.24.0",
    "vite": "^5.4.11"
  }
}
